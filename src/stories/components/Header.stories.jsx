import Header from '../../components/Header';

export default {
  title: 'Example/Header',
  component: Header,
};

function Template() {
  return <Header />;
}

export const Component = Template.bind({});
