import Button from '@/components/Button';

export default {
  title: 'Example/Button',
  component: Button,
};

function Template() {
  return <Button />;
}

export const Component = Template.bind({});
